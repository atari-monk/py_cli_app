{
    "commands": {
        "clear": { "description": "Clear the console screen" },
        "example": {
            "description": "Prints a simple example message form commands folder"
        }
    },
    "example": {
        "example": {
            "description": "Prints a simple example message form example folder"
        }
    }
}
